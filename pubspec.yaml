name: my_mikhailovka
description: Application developed special for Mikhailovka with love.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  http: ^0.12.0+1
  http_retry: ^0.1.1+3
  flutter_map: any
  tuple: ^1.0.2
  rubber: ^0.2.6


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true